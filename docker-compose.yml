version: '3.7'
services:
  gostack_postgres:
    image: postgres
    container_name: gostack_postgres
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
